import Stack from "@mui/material/Stack";

import { styled } from "@mui/material/styles";

export const LanguageSwitchStack = styled(Stack)(({ theme }) => ({
  margin: "0 auto",
  width: "150px",
  alignItems: "center",
}));

export const stackSx = { margin: "0 auto", width: "150px", alignItems: "center" };
