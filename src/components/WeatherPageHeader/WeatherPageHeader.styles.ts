import Typography from "@mui/material/Typography";
import { styled } from "@mui/material/styles";

export const TabTitleTypography = styled(Typography)(({ theme }) => ({ textAlign: "center", margin: "0 auto" }));

export const typographySx = { textAlign: "center", margin: "0 auto" };
